import { DBConfig } from "./src/db/connection"

export const _DB_CONFIG: DBConfig = {
    host: 'bd.procced.com.br',
    user: 'jean',
    password: 'pwjean',
    database: 'jean'
  }

  export const _WS_PORTA: number = 3001